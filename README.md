# Docker MongoDB configurado

Esta é uma imagem Docker já configurada. Foi baseada no código do tutum/mongodb, um docker configurado utilizando ubuntu e mongodb.

Para rodar, é preciso utilizar o podman exec -it <db> <comando mongosh de dentro do container>.

